<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 使用注解的包，包括子集 -->
    <context:component-scan base-package="cfw"></context:component-scan>

    <!-- 当使用 JUnit 测试时，不扫描common包下的内容 -->
    <!--<context:component-scan base-package="cfw">
    	<context:exclude-filter type="regex" expression="cfw\.common\.*.*"/>
    </context:component-scan>-->

	<!-- it seems useless -->
	<!-- <mvc:default-servlet-handler/> -->
	
	<!-- <mvc:resorce mapping="/Deals/res/**" location="/Deals/res/" /> -->

    <!-- 视图解析器 -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/admin/" />
        <property name="suffix" value=".html"></property>
    </bean>

    <!-- 支持对象与json的转换。 -->
    <mvc:annotation-driven></mvc:annotation-driven>

</beans>